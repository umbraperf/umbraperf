/* 
    pub fn api() -> RecordBatch {

        for element in restful_str {

            if filter

            
            if special_operator


            if selects
            
        }
        
    }
 */
    // FILTER
    // /?operator=132
    // COLUMN
    // /operator
    // count?operator